# 变更日志 (Changelog)

本项目的所有重要更改都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 1.0.3

本节 = 2026-09-11 工程化批次 + 09-12 界面主题与子分类 + 09-13 子分类收敛（删漫画 / 删音乐「其他」）与网文独立目录。

### 新增

- **现代风格界面**：全面采用现代主题——层级阴影、卡片悬停浮起、**分段式页签**（胶囊容器内一枚描边药丸跟随当前页签平滑滑动），与 Obsidian 自身的明暗主题相互独立（可任意组合）。
- **悬浮提示延迟显示**：悬停按钮 / 图标约半秒后才出现提示气泡，快速扫过一排按钮不再连续弹泡；移开、滚动或点击立即消失。
- **书籍分类**：阅读页签新增【全部 / 文学 / 网文】子分类（「出版」已更名为「文学」）；顶部计数跟随所选子分类显示（如「4 本网文」）；书籍分类的选择统一收进搜索框类型下拉，历史条目未指定时默认归入「文学」。
- **搜索框类型可选「网文」**：添加条目的搜索框类型下拉新增「网文」选项，选中即按书籍源搜索（豆瓣主源 + Open Library / Google Books），头部同步显示当前类型与参与源；编辑书籍条目时下拉同样显示「网文」。书籍分类的选择统一收进这个下拉，表单内原有的「书籍分类」选择行已移除。下拉顺序为：文学、网文、动画、电视剧、电影、游戏、音乐（原「书籍」项在此下拉中显示为「文学」）。
- **网文视图添加自动选中**：阅读页签聚焦「网文」子分类时点「＋ 添加」，搜索框自动选中对应类型（聚焦「文学」或「全部」保持默认文学）。
- **网文笔记独立目录**：网文条目的详情笔记写入 `笔记/novel/`（文学条目仍在 `笔记/book/`），两类书不再混放在同一文件夹；升级后已存在的网文笔记会自动搬到新目录，引用同步更新（幂等，重复升级不会重复搬动）。
- **网文专属字段**：网文的编辑表单改为 **上架年 / 作者 / 题材 / 元数据章数**（不再有出版社、ISBN 与目录三个框）；详情笔记同步按网文口径呈现（「上架年」「章数」，不再渲染出版社 / ISBN / 译者 / 出品方 / 装帧 / 定价 / 丛书与「目录」小节）。文学条目字段完全不变。
- 切换「界面主题」即时生效，无需重载或重启。
- **批量操作先确认再执行**：批量修改状态 / 评分 / 标签时先弹确认框，列明「将对多少项、改什么、会一并更新详情笔记」，确认后才执行（确认前可随时取消，已勾选内容不受影响）。删除操作原有二次确认不变。
- **数据源失败时给出「怎么办」**：搜索失败提示不再只说「请检查设置」，而是按失败原因给出具体处方——未配置凭据的指路设置页、网络问题的建议稍后重试或换源、豆瓣风控的说明等待几分钟。
- **封面下载失败不再无声无息**：自动下载封面失败时会提示已保留网络地址、预览可能空白，并指引用手动方式（拖入图片 / 右键更换本地图片）补封面。
- **统计页趋势更省心**：月度看完趋势图只画到当前月份（未到的月份不再拖一条贴底平线，刻度淡显）；鼠标悬停任意月份，即显示该月各类型（书 / 影视 / 游戏 / 音乐）的数量。
- **搜索框加了放大镜图标**：库搜索框、添加条目的搜索框、金句弹窗的搜书框统一补上图标；添加条目搜索时若关键词为空，会在原位给出提示，而不是静默无反应。

### 变更

- **书籍子分类「出版」更名「文学」**：仅显示名变化（阅读页签子分类、搜索框下拉、顶部计数），条目数据不受影响。
- **页签更名**：主视图页签「书籍」改为「阅读」（仅页签显示名；表单、统计等场景的类型名称保持不变）；音乐页签保持「音乐」。
- **漫画子分类与漫画源移除**：阅读页签不再有【漫画】子分类，搜索框类型下拉不再有「漫画」选项，设置页「数据源启用」不再有「漫画」分类行（配套的 Bangumi 书籍类目搜索一并移除）。此前标记为「漫画」的条目**不丢**，升级后自动并入「文学」显示（条目的标题、评分、笔记等全部保留，只是不再单列视图）。
- **音乐子分类移除**：音乐页签不再有【全部 / 音乐 / 其他】子分类行，条目表单不再有「音乐分类」选择；此前标记为「其他」（有声书、播客等）的音乐条目自动并入「音乐」，条目本身不受影响。
- **网文不再保存出版侧数据**：网文条目保存时会清空其 **出版社 / ISBN / 目录** 三项存量字段（出版信息对网文无意义，属一次性清理，不可恢复）；文学条目的这三项照常保存。
- **状态徽标颜色加深**：「想看 / 在看 / 已看 / 存档」四色底色整体加深，白色小字在浅色与深色主题下都清晰可读（此前浅色主题下对比度不足）。
- **界面配色整体按无障碍标准校准**：全部界面颜色按 WCAG 对比度标准实测校准——正文颜色浅色主题不低于 4.5:1、深色主题同步复核；进度条三档颜色还原为独立配色。
- **评分与星级恢复原色**：评分数字与星级恢复原本的暖金色（各主题共用同一套颜色，不再随主题变色）；这是在「颜色一致」与「浅色主题下对比度略低」之间的刻意取舍。
- **顶部页签「计划表」更名「计划」**：仅页签显示名变化，页签内的「追番表 / 排期表」视图与「打开计划表」命令名称保持不变。
- **现代主题圆角回退与原生一致**：现代主题曾把按钮 / 输入框圆角放大到 10px、卡片 / 弹窗放大到 16px，上手观感过于夸张——现回退为与原生主题完全一致（6 / 8px）；现代风格由阴影、悬停动效与页签胶囊承担。
- **界面主题切换入口移除**：现代风格成为唯一界面主题，不再提供原生 / 现代切换（偏好原生的老用户自动迁移）；同时卡片悬停上浮幅度从 7px 收敛到 3px，动效更克制。
- **统计页「四库分区」更名「概览」**：仅标题文案变化，内容与位置不变（仍是数据主体区）。
- **编辑弹窗按钮文案与观感微调**：编辑已有条目时保存按钮显示「更新笔记」（新增时仍为「保存并生成笔记」）；「添加摘抄 / 记录游玩」按钮改为恒实心底色；底部操作栏去掉整行背景，只留按钮悬浮，并排按钮不再像一条色带。

### 修复

- **网文条目保存后落错分类**：通过搜索框选「网文」保存的书籍条目，此前会被静默归入「文学」（保存环节漏掉了子分类字段）；现已修复，此后保存的条目正确归入所选分类。⚠️ 此前已保存的网文条目需重新编辑保存一次才会归位。
- **音乐页签数量显示错误**：音乐页签顶部曾显示「8 项媒体」，现正确显示「8 首音乐」（其余五类页签不受影响）。
- **现代主题页签切换无滑动动画**：现代主题分段式页签的描边药丸切换时原位不动——指示器样式串缺 `transform:` 属性名，被浏览器按非法声明静默丢弃（不报错，仅宽度生效）；现补全属性名，切换页签时药丸平滑滑动到新页签。
- **窄栏下页签条横向滚动后指示器错位**：页签条溢出出现横向滚动时，指示器会向左漂移、脱离选中页签（定位误减了容器滚动量；指示器本就随内容一起滚动，坐标系无需补偿）——现修正，任何滚动状态下指示器都贴合选中页签。

### 说明

- 页签**显示图标**（定稿时曾定为纯文字，按上手反馈恢复）；选中态由描边药丸平滑滑动标识。
- 内置阅读器（TXT/EPUB/PDF）与视频播放器**不参与**主题化，维持原有沉浸式外观。
- 沿用旧版「色彩主题」设置的用户不受影响；该历史设置项已自动归一，不会在升级后突然出现类型色条。
- 仓库侧完成一轮开源化整理（不影响插件功能）：README 增加徽章、FAQ 9 组与「已知问题与限制」；补齐 `.github/` 模板八件套（issue 模板 / PR 模板 / CONTRIBUTING / CODE_OF_CONDUCT / FUNDING 等）。

## [1.0.2] - 2026-09-10

1.0.2 是 1.0.1 之后的第二个增量版，分两批落地：**09-09** 一批阅读器精度修复（EPUB 进度口径、目录联动、读完记 100%）、豆瓣封面统一高清化、删除弹窗与右键菜单等界面细节收敛，并完成两项自动迁移的目录整理（笔记子目录英文化、进度文件可读名）；**09-10** 补齐数据与 AI 能力——统计页「今日记录 + 一键写日记」、AI 智能摘要（一句话总结 / 核心看点）、笔记 frontmatter 对齐 Dataview、AI 翻译与总结服务可选服务商与自定义提示词，以及一批浏览与菜单交互优化。迁移类改动升级后自动生效，无需手动处理；AI 相关功能需自备服务商 API Key（设置 → 服务集成 → AI 翻译与总结）。

### 新增

- **豆瓣封面统一高清化**：所有类型从豆瓣获取封面时自动取高清档（此前搜索级缩略小图 70~100px 会直接入库导致海报墙发糊）；详情页 JSON-LD 缺图时以 og:image 兜底，封面不再模糊或缺失。
- **EPUB 章节自动解析**：编辑书籍进度时选择本地 EPUB 文件即可自动解析总章节数（与 TXT 按章、PDF 按页并列），进度单位随文件类型自动切换显示「章 / 页」。
- **动态面板（统计页）**：统计页新增「动态」面板，可切 **日 / 周 / 月 / 年**（日历口径：周从周一起算并标「第 N 周」、月为当月、年为当年）。**日视图**逐条列出（`HH:mm` + 状态 + 作品，精确到几点几分）；**周 / 月 / 年视图改为周期汇总**，并**逐级细分**——周视图按「日」分块（`9月10日`）、月视图按「周」分块（`第 37 周（9月7日–9月13日）`）、年视图按「月」分块（`9月`）；每块内归并成「已读《A》《B》」「在看《C》」「新增《D》」「追更《E》S1E2」这样的行，一眼看清「这周 / 这个月 / 今年看了什么」；条目多时面板内滚动，点任意作品可跳对应笔记。
- **一键记录到日记（范围跟随切换）**：动态面板右侧的记录按钮随 日/周/月/年 切换变为「记录今天 / 记录本周 / 记录本月 / 记录今年」——周/月/年视图把该周期的**分块汇总**（与面板所见一致）写成打卡区块，区块标题按范围区分（`2026-09-10` / `第 37 周（2026-09-07–09-13）` / `2026-09` / `2026`），不同范围的区块互不覆盖；文件始终写入**当天日记**，重复点击只更新不重复追加。，追加进**当天日记**（`## ReelLudic 打卡 · 日期` + `> [!reelludic]` 呼号块：逐条 `HH:mm 状态《标题》` + 新增条目清单一览）。重复点击只更新该区块、不会重复追加；日记位置自动跟随核心「日记」插件（未配置时用 `日记/YYYY-MM-DD.md`），目录不存在会自动创建。
- **AI 智能摘要**：编辑表单标题旁新增「总结摘要」块，可一键让 AI 生成**一句话总结**（≤40 字、不剧透）与**核心看点**（建议 3 条），也可自己手填；结果随笔记生成写入「## 一句话总结 / ## 核心看点」两节，之后在笔记里手改也能被表单读回。摘要用的 AI 服务商可单独选择。
- **笔记 frontmatter 对齐 Dataview**：笔记头部新增 `tags`（含类型标签，如 `movie` / `book`，可与自定义标签并用）、`created` / `updated` 日期、`progress_percent` 进度百分比——可直接用 `TABLE rating, status FROM #movie WHERE rating >= 4 SORT created DESC` 这类查询。
- **AI 服务可选服务商 + 自定义提示词**：设置页「AI 翻译与总结」内，「翻译服务」与「总结服务」的服务商**各自独立**选择（GLM-4-Flash / DeepSeek v4 Flash，两者共用同一组 API Key）；两项下各有一个**提示词**输入框，留空即用默认提示词。

### 变更

- **详情笔记文件夹改英文名（自动迁移）**：`笔记/电影|电视剧|动画|书籍|游戏|音乐` → `笔记/movie|teleplay|animation|book|game|music`，旧笔记文件夹与内部路径随更新自动迁移；界面类型名称仍显示中文，不受影响。
- **阅读进度文件显示书名（自动迁移）**：进度/书签 JSON 文件名由内部编号（`e_xxx.json`）改为 `{书名}-阅读进度-{编号}.json` / `{书名}-书签-{编号}.json`，一眼看出对应哪本书；编号尾段保留以关联条目、避免同名书冲突。已有文件自动改名，进度与书签内容不受影响。
- **EPUB 全书进度按正文字数计算**：此前按包含 HTML 标签与封面壳的原文长度加权，标签密的短章被高估——现改为纯文本字数，进度更真实。
- **EPUB 目录只收真正的章节**：目录解析不再混入 landmarks / page-list 等非章节项。
- **书籍编辑表单排版**：作者与题材并排两列；题材输入框 label 统一为「题材」（与影视 / 动画 / 游戏一致）。
- **阅读器顶栏按钮提示统一自绘气泡**：书签 / 批注 / 翻译 / 高亮 4 个标注模式按钮的悬停提示由系统原生黄条改为与全项目一致的自绘深色气泡。
- **设置页「关于」入口文案**：「支持作者」→「支持 ReelLudic」。
- **文件关联不再二次弹窗**：所有类型（影视 / 书籍 / 游戏 / 音乐）点「浏览…」直接唤起系统文件管理器，不再先弹「从库中选择 / 从系统浏览」的二选一（少一步点击；vault 内的文件在系统对话框里同样可选）。路径仍按类型自动归一，会话内记住上次浏览目录。
- **设置区更名「AI 翻译与总结」**：原「阅读器翻译管理」更名为「AI 翻译与总结」（现在同时管理划词翻译与 AI 摘要两个服务）。
- **打卡入口只有一处**：设置页的日记文件夹 / 文件名格式 / 立即记录三行移除，打卡只在统计页「今日」面板触发。

### 修复

- **EPUB 目录联动错位**：阅读时目录高亮/跳转此前按目录列表序号对齐章节，遇到「目录与正文章节非一一对应」的书会滑到错误章节——现按真实章节地址匹配定位。
- **读完/标记已读进度不是 100%**：读到一章末尾或对书籍标记「已读」后，进度可能停在 97-99%（进度条差一点不满）——现正确记为 100%。
- **海报墙右键菜单被卡片遮挡**：靠近视口边缘的卡片呼出右键菜单时，菜单会被卡片悬停遮罩盖住无法操作。
- **悬停提示双气泡重叠**：编辑表单浮层内按钮、「观看」与「从文件夹检索剧集」等按钮悬停时会同时弹出两个重叠提示（自绘气泡 + Obsidian 官方气泡互相覆盖）——现同一目标只保留一个提示。
- **删除弹窗视觉过重**：删除类确认弹窗去掉红色圆形警示底块，保留 ⚠️ 图标，观感更轻。
- **右键菜单在屏幕边缘跑出视口**：在海报墙右下角呼出右键菜单时，菜单可能被裁掉或整个跑出屏幕（边缘判断用了错误的坐标系）——现在会先翻到鼠标另一侧、再收进视口内，菜单过高时改为内部滚动；编辑表单里封面图右键菜单同样处理。
- **菜单在视口边缘的重复监听**：反复呼出右键菜单会累积关闭监听（曾需多按一次 Esc 才关得掉），现改为打开时绑定、关闭时移除。
- **月度趋势图「少了类型折线」**：游戏 / 音乐当年 0 条时也会画出一条落在 0 基线的直线，与书籍的贴底线重叠成一条，看起来像「只有一两条线」——现改为**0 条的类型不画线**，图例加色点并把无数据类别灰显标 `0`，一眼看清哪些类型本年没有完成记录。
- **统计页动态列表横排错乱**：动态条目原为横向排列（像一排标签、多了就折行）——现改为一行一条、时间列对齐、超出高度内部滚动。

### 移除

- EPUB 阅读器顶栏「⇆ 双页」切换按钮（阅读布局以连续滚动 / 翻页为主，双栏入口不再暴露）。
- 删除类确认弹窗的红色圆形警示底块。
- 「从库中选择 / 从系统浏览」文件来源二选一弹窗（改为直连系统文件管理器）。
- 设置页「数据管理」下的打卡日记文件夹 / 文件名格式 / 记录今日到日记三行（打卡统一在统计页）。

## [1.0.1] - 2026-09-06

1.0.1 是 1.0.0 正式版后的第一个增量版本：围绕「收藏管理效率 + 阅读体验」做了一批小版本优化，重点包括动画/剧集批量关联本地剧集文件、PDF 阅读器进度与目录体验修复、添加与排序细节打磨、以及全项目悬停提示风格统一。

### 新增

- **添加条目默认类型跟随当前页签**：在「动画 / 电视剧 / 电影」分类下点「＋ 添加」，新增表单默认就是该分类（此前无论哪个页签都默认电影）。
- **右键「去关联」直达关联弹窗**：条目缺少观看/阅读/播放/启动入口时，右键主操作直接弹出轻量关联窗口——书籍/游戏/音乐选择本地文件即存即生效；影视可逐集填写网络地址或本地视频；**动画/电视剧支持「从文件夹检索剧集」**：选一个剧集文件夹，自动按文件名集号（第N集 / S01E0N / 01…）识别并批量填入对应集的本地路径，几十集一次导入，可跳过已填集、保存后立即可播。
- **电影「观看」按钮显示文字**：编辑电影条目时，观看链接按钮由纯 ▶ 图标改为「▶ 观看」，与书籍「▶ 阅读」风格一致。
- **文件选择器记住上次目录**：连续为剧集逐集选择本地文件时，每次打开系统选择器都定位到上次选中的文件夹。
- **海报墙游戏卡片时长占位**：游戏未填写游玩时长时，卡片进度行显示「-」占位（不再整行塌陷，与相邻卡片高度一致）。

### 变更

- 编辑表单布局：动画/电视剧的「总集数」与「当前进度（S/E）」改为同行并排；电影不再显示总集数输入（单集语义）。
- 全类型排序支持反向：标题 A-Z/Z-A、最近更新、发布日期、大众评分、个人评分均成对提供正反方向。
- 海报墙题材行始终渲染：题材为空显示「—」占位（同作者/年份占位风格），空题材卡片不再缺行。
- 悬停提示全项目统一为**自绘气泡**（深色小气泡、固定在元素正下方）：不再出现系统原生黄条与其它提示并存的观感分裂；无障碍标签（aria-label）保留仅供读屏。
- 「隐藏插件内滚动条」设置只作用于主界面与编辑弹窗——TXT/EPUB/PDF 阅读器滚动条始终显示。

### 修复

- **PDF 阅读器进度无法持久化**：此前关闭阅读器后进度被重置回第 1 页、海报墙也不显示该书的阅读进度；现在关闭即保存、重开恢复、海报墙进度同步显示。
- **PDF 阅读器目录不联动当前章节**：滚到第 5 章时左侧目录自动高亮第 5 章并滚动到可见位置。
- 扫描版 PDF（CCITT/JBIG2 传真压缩）整页空白：pdfjs-dist 固定 v4.10（传真解码纯 JS 内联，不依赖外部 WASM）。
- 部分 EPUB 无标准目录（缺 nav/NCX 或 NCX 变名）时目录显示文件名而非章节名——现按多级兜底提取章节名。
- 「添加动画条目」选中豆瓣结果无法回填题材/导演/主演；Bangumi 结果无法回填导演（persons 接口为裸数组，兼容两种响应形态）。

## [1.0.0] - 2026-09-05

首个正式版（1.0.0）：在 0.5.0 聚合版基础上完成统计页整体重构、阅读进度可手动维护、排期表与列表视图交互打磨，并做了一轮全项目 UI 一致性收敛。

### 新增

- **统计页完全重构（数据优先、克制裁剪）**：页面改为「页眉动作 → KPI 数字行 → 月度趋势 × 今年动态 → 四库分区 → 快捷行」；删除视觉噪音（霓虹热力/玻璃拟态/渐变发光），回归 Obsidian 原生变量风格。
- **往年报告入口**：统计页可列出并打开历年生成的年度总结，一键生成今年总结。
- **影视进度手填**：剧集/动画的编辑表单新增「当前进度 S[季] E[集]」输入，可手动维护看到第几集，列表/月历/追番表展示同步。
- **影视本地视频内嵌播放（设置可开关）**：设置页新增「内置播放器打开视频文件」开关（默认关闭）。开启后影视条目的本地视频（电影/剧集/动画）可在 Obsidian 内直接内嵌播放，支持同一部作品多集在播放器内上/下一集切换；兼容 mp4/webm/mov/ogv，mkv 等 Chromium 无法解码的格式自动转交系统播放器（关闭开关则一律系统播放器）。

### 变更

- 列表视图「进度」列仅「在看」状态显示 SxEy 与进度条——想看/已看/存档不再误显示历史残留进度。
- 列表/追番表/音乐行/海报墙操作按钮 hover 反馈统一：默认灰，悬停主题色并放大（单一缩放口径）。
- 设置页「阅读器翻译管理」：服务商折叠标题改为「智谱清言 / DeepSeek」，删除冗余提示文案。
- 排期表月历/周历采用统一高度基准，切换视图不再跳动；待排条目侧栏限高、内部滚动。

### 修复

- 周视图日期格此前不可点击——现与月历一致，点击当天弹出计划详情；当天弹窗「清除排期」后保持打开，可连续清除多条。

### 移除

- 年度目标能量环（原 localStorage 键随 UI 移除）；统计页霓虹热力图与玻璃拟态装饰块。

## [0.5.0] - 2026-09-04

首个聚合发布版，包含此前累积的阅读器界面重排与阅读能力扩展。

### 新增

- **阅读器界面重排（仿 Readest 沉浸式）**：TXT/EPUB 阅读器重构为软件式仿真界面——去除原生弹窗框、顶栏半透明悬浮并自动隐藏（鼠标靠顶唤出）、正文阅读栏限宽居中（TXT 680px / EPUB 720px）。
- **书签体系**：阅读时按章节+进度收藏位置书签，独立落盘持久保存；侧栏书签列表支持跳转/右键删除。
- **选中文字动作条**：在正文选中文字弹出浮动工具条，提供书签、摘抄、高亮、翻译四个动作。
- **摘抄进笔记**：选中文字可确认后写入书目笔记的「摘抄」区块（带页码/心得）。
- **AI 划词翻译**：选中文字一键翻译为就地译文浮层，自动中英互译。服务双源可选（智谱 GLM-4-Flash 免费 / DeepSeek），可在设置页配置 API Key 并一键测试连接。
- **页内持久高亮**：选中文字一键标黄，自动写入笔记独立「高亮」区块；重开/切章自动还原黄标；批注面板可浏览、跳转、单条删除或一键清除全部。
- **翻页模式**：阅读器可在「连续滚动」与「翻页式」两种模式间切换；翻页式将正文按屏幕分页，支持按钮/方向键/滚轮/点屏两侧整屏切换，进度按章内百分比记忆。
- **标注模式锁**：顶栏「书签 / 批注 / 翻译 / 高亮」四键点击后进入保持变色的激活状态，此后拖选正文文字即自动执行对应标注；再点退出。
- **数据源扩展**：搜索集成豆瓣、TMDB、Bangumi、Open Library、Google Books、Steam、MusicBrainz、iTunes、OMDb、AniList 等数据源，书籍/游戏/音乐/影视/动画各类型可自选来源链。
- **设置页服务集成**：集中的 API 凭据管理与各类型数据源启用/排序配置。

### 变更

- 阅读器整体从桌面浮窗改为主流软件式阅读体验（去原生框、悬浮顶栏、沉浸式跟随系统明暗主题）。
- 翻译服务从自托管 DeepLX 端点切换为云 AI 大模型（智谱 / DeepSeek），并支持在设置页配置。

### 修复

- 关联 TXT 阅读进度在编辑表单不显示的问题：进度落库补齐「页」数据，旧数据打开自动回填。

### 移除

- 阅读器底部与顶栏原有的行距±/批注按钮，改为下拉菜单与选中动作条（对应能力仍可用）。

<!-- Keep a Changelog：按 `## [版本] - 日期` 逐版本追加；每个版本下用「新增/变更/修复/移除」分类。发布新版本时把「未发布」内容落版本并把本文件版本号与 manifest.json 同步。 -->
